{
  "name": "zeros-shop",
  "version": "1.0.0",
  "description": "小玲の運動系列選品 — Zero's",
  "private": true,
  "scripts": {
    "start": "serve -s . -l tcp://0.0.0.0:${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "engines": {
    "node": ">=18"
  }
}
